import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.foodplanning.app',
  appName: 'food-planning',
  webDir: 'dist/spa',
  bundledWebRuntime: false
};

export default config;
